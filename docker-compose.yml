services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: goc
      POSTGRES_PASSWORD: goc
      POSTGRES_DB: guild_of_comfort
    ports:
      - "5432:5432"
    volumes:
      - goc_pgdata:/var/lib/postgresql/data

volumes:
  goc_pgdata:
